{
  "name": "DeVote",
  "version": "1.0.0",
  "description": "A Decentralized Voting App.",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "build:ui": "yarn workspace ui run build",
    "build": "yarn build:ui",
    "start:ui": "yarn workspace ui run start",
    "start:backend": "firebase emulators:start",
    "start": "yarn start:backend & yarn start:ui"
  },
  "dependencies": {
    "firebase-tools": "^13.13.2"
  }
}
